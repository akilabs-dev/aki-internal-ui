/**
 * Paste into the CSS tab when the component only needs standard Tailwind utilities.
 */
export const tailwindCopyCss = `/* Tailwind via npm package — see Usage → HTML project for install steps */

/* 1. npm install tailwindcss @tailwindcss/cli */
/* 2. input.css: @import "tailwindcss"; */
/* 3. npx @tailwindcss/cli -i ./src/input.css -o ./public/css/app.css */
/* 4. <link rel="stylesheet" href="/css/app.css" /> */

/* This component uses standard utilities only — no extra CSS after Tailwind is built. */`
